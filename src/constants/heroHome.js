/**
 * Homepage hero — video + poster paths under `public/`.
 * Replace filenames here if you swap assets (keep files in `public/`).
 */
export const HERO_HOME_MEDIA = {
  /** Primary hero loop (muted, for autoplay) */
  video: 'azilherosction.mp4',
  /** Poster + image fallback (same file is fine) */
  poster: 'roof2.webp',
  fallback: 'roof2.webp',
}
